// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5) // 3
resultado = Math.ceil(2.2) // Redondea hacia arriba (3)
resultado = Math.floor(2.9) // Redondea hacia abajo (2)
resultado = Math.sqrt(144); // Raíz cuadrada
resultado = Math.abs(-200); // Valor absoluto
resultado = Math.max(1, 5, 3, 10, 100, 70); // Máximo
resultado = Math.min(1, 5, 3, 10, 100, 70); // Mínimo
resultado = Math.random(); // Número aleatorio, siempre < a 0
resultado = Math.floor( Math.random() * 30 ); // Crea un número aleatorio entero menor a 30